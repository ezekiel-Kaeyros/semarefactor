export const CalendarCredentials = {
    clientId: process.env.CALENDAR_CLIENT_ID,
    clientSecret: process.env.CALENDAR_CLIENT_SECRET,
    redirectUrl: process.env.CALENDAR_REDIRECT_URL
}

export const CalendarUrl = {
    authScope: 'https://www.googleapis.com/auth/calendar'
}

export const CalendarTimeZone = {
    time: 'America/Los_Angeles'
}